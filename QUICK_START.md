# راهنمای سریع راه‌اندازی Cutup

## 🚀 راه‌اندازی سریع (5 دقیقه)

### 1. تنظیم Backend در Vercel

```bash
# نصب Vercel CLI (اگر نصب نیست)
npm install -g vercel

# لاگین
vercel login

# تنظیم API Key
vercel env add OPENAI_API_KEY production
```

وقتی از شما خواست، این مقدار را وارد کنید:
```
YOUR_OPENAI_API_KEY
```

### 2. Deploy کردن Backend

```bash
vercel --prod
```

بعد از deploy، URL را کپی کنید (مثلاً: `https://cutup-api.vercel.app`)

### 3. تنظیم API URL در افزونه

✅ API URL در فایل `popup.js` تنظیم شده است:

```javascript
const API_BASE_URL = 'https://cutup-a0p9oqk9z-hamidreza-askarizadehs-projects.vercel.app';
```

### 4. ساخت آیکون‌ها (موقت)

برای تست سریع، می‌توانید از یک تصویر ساده استفاده کنید:

```bash
# در پوشه icons/ سه فایل PNG بسازید:
# - icon16.png (16x16)
# - icon48.png (48x48)  
# - icon128.png (128x128)
```

یا از یک سایت آنلاین مثل https://www.favicon-generator.org/ استفاده کنید.

### 5. نصب افزونه در Chrome

1. Chrome را باز کنید
2. به `chrome://extensions/` بروید
3. "Developer mode" را فعال کنید (گوشه بالا-راست)
4. روی "Load unpacked" کلیک کنید
5. پوشه `cutup` را انتخاب کنید

### 6. تست

1. روی آیکون افزونه کلیک کنید
2. یک فایل صوتی (MP3, WAV, etc.) انتخاب کنید
3. روی "خلاصه‌سازی" کلیک کنید
4. منتظر نتیجه بمانید (30-60 ثانیه)

## ⚠️ نکات مهم

- API Key در فایل `.env.local` برای development است (در .gitignore است)
- برای production، حتماً از Environment Variables در Vercel استفاده کنید
- استخراج صوت از یوتیوب در نسخه فعلی پشتیبانی نمی‌شود
- فایل‌های بزرگ ممکن است زمان بیشتری ببرد

## 🐛 عیب‌یابی

### خطای "API Key is not set"
- مطمئن شوید که `OPENAI_API_KEY` را در Vercel تنظیم کرده‌اید
- بعد از تنظیم، دوباره deploy کنید: `vercel --prod`

### خطای CORS
- مطمئن شوید که API در Vercel deploy شده است
- URL را در `popup.js` بررسی کنید

### افزونه کار نمی‌کند
- Console را باز کنید (F12)
- به تب Extensions بروید
- روی "Errors" کلیک کنید
- خطاها را بررسی کنید

## 📞 پشتیبانی

اگر مشکلی پیش آمد، فایل `SETUP.md` را برای راهنمای کامل‌تر ببینید.

