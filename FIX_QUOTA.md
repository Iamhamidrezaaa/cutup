# رفع مشکل Quota OpenAI

## 🔍 وضعیت فعلی

خطای `ReferenceError` برطرف شد ✅  
حالا مشکل اصلی **Quota OpenAI** است.

## ✅ مراحل رفع مشکل

### 1. بررسی حساب OpenAI

به این آدرس‌ها بروید و وضعیت را بررسی کنید:

#### الف) بررسی Usage و Quota:
- https://platform.openai.com/account/usage
- بررسی کنید که آیا اعتبار باقی مانده است یا نه

#### ب) بررسی Billing:
- https://platform.openai.com/account/billing
- بررسی کنید که:
  - ✅ روش پرداخت فعال است
  - ✅ اعتبار کافی وجود دارد
  - ✅ محدودیت استفاده اعمال نشده

#### ج) بررسی API Keys:
- https://platform.openai.com/api-keys
- مطمئن شوید API Key فعال است

### 2. اگر Quota تمام شده است

#### گزینه A: اضافه کردن اعتبار
1. به https://platform.openai.com/account/billing بروید
2. روی "Add payment method" یا "Add credits" کلیک کنید
3. اعتبار اضافه کنید

#### گزینه B: بررسی محدودیت‌ها
1. به https://platform.openai.com/account/limits بروید
2. بررسی کنید که آیا محدودیت استفاده اعمال شده است
3. اگر محدودیت دارید، آن را افزایش دهید یا حذف کنید

### 3. اگر Quota فعال است اما هنوز خطا می‌دهد

#### بررسی 1: API Key درست است؟
```bash
# بررسی Environment Variables در Vercel
vercel env ls
```

مطمئن شوید که `OPENAI_API_KEY` برای Production تنظیم شده است.

#### بررسی 2: Deploy مجدد
بعد از هر تغییر در Environment Variables، باید deploy کنید:
```bash
vercel --prod
```

#### بررسی 3: تست مستقیم API Key
می‌توانید مستقیماً API Key را تست کنید:

```bash
curl https://api.openai.com/v1/models \
  -H "Authorization: Bearer YOUR_API_KEY"
```

اگر 401 می‌دهد، API Key مشکل دارد.  
اگر 200 می‌دهد اما quota error می‌دهد، واقعاً quota تمام شده است.

### 4. بررسی لاگ‌های Vercel

```bash
vercel logs cutup-1ewgva5y4-hamidreza-askarizadehs-projects.vercel.app
```

در لاگ‌ها باید ببینید:
- `HAS_KEY true` ✅
- `KEY_PREFIX sk-proj` ✅
- خطای quota از OpenAI

### 5. راه‌حل موقت (اگر نمی‌توانید quota را اضافه کنید)

اگر نمی‌توانید quota را اضافه کنید، می‌توانید:

1. **از API Key دیگری استفاده کنید** (اگر دارید)
2. **صبر کنید تا quota reset شود** (اگر محدودیت روزانه/ماهانه دارید)
3. **از سرویس جایگزین استفاده کنید** (مثلاً Google Speech-to-Text)

## 📋 چک‌لیست

- [ ] به https://platform.openai.com/account/usage رفته‌ام
- [ ] به https://platform.openai.com/account/billing رفته‌ام
- [ ] اعتبار/روش پرداخت را بررسی کرده‌ام
- [ ] اگر quota تمام شده، اعتبار اضافه کرده‌ام
- [ ] API Key را در Vercel بررسی کرده‌ام
- [ ] بعد از تغییرات، deploy مجدد انجام داده‌ام

## 🎯 بعد از رفع Quota

بعد از اینکه quota را فعال کردید:

1. **Deploy مجدد** (اگر Environment Variable تغییر کرده):
   ```bash
   vercel --prod
   ```

2. **تست مجدد**:
   - افزونه را reload کنید
   - یک فایل صوتی کوچک تست کنید
   - باید کار کند! ✅

## 💡 نکات مهم

- Quota ممکن است به صورت **روزانه**، **ماهانه**، یا **کل** محدود شده باشد
- بعضی حساب‌های OpenAI **free tier** دارند که محدودیت دارند
- اگر از **organization** استفاده می‌کنید، quota ممکن است در سطح organization تنظیم شده باشد

