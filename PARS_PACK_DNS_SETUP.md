# راهنمای تنظیم DNS در پارس پک برای cutup.shop

## 📍 مراحل تنظیم DNS در پنل پارس پک

### مرحله 1: ورود به پنل پارس پک

1. به سایت [parspack.com](https://parspack.com) بروید
2. روی "ورود" کلیک کنید و وارد شوید
3. به بخش "مدیریت دامنه" یا "Domain Management" بروید

---

### مرحله 2: پیدا کردن دامنه cutup.shop

1. در لیست دامنه‌ها، `cutup.shop` را پیدا کنید
2. روی دامنه کلیک کنید یا به بخش "مدیریت DNS" بروید

---

### مرحله 3: تنظیم DNS Records

در بخش "مدیریت DNS" یا "DNS Management"، رکوردهای زیر را اضافه کنید:

#### گزینه 1: اگر می‌توانید DNS Records را مستقیماً ویرایش کنید:

**رکورد A برای دامنه اصلی:**
```
Type: A
Name: @ (یا خالی بگذارید)
Value: 195.248.240.108
TTL: 3600 (یا Auto)
```

**رکورد A برای www:**
```
Type: A
Name: www
Value: 195.248.240.108
TTL: 3600 (یا Auto)
```

#### گزینه 2: اگر فقط Nameserver می‌توانید تغییر دهید:

اگر پارس پک امکان ویرایش مستقیم DNS Records را نمی‌دهد، باید Nameserver را تغییر دهید.

**استفاده از Cloudflare (پیشنهادی - رایگان):**

1. به [cloudflare.com](https://dash.cloudflare.com/sign-up) بروید و ثبت‌نام کنید
2. روی "Add a Site" کلیک کنید
3. دامنه `cutup.shop` را وارد کنید
4. Plan رایگان را انتخاب کنید
5. Cloudflare به شما دو Nameserver می‌دهد، مثلاً:
   - `alice.ns.cloudflare.com`
   - `bob.ns.cloudflare.com`

6. در پنل پارس پک:
   - به بخش "Nameservers" یا "تنظیمات Nameserver" بروید
   - Nameserver های Cloudflare را وارد کنید
   - ذخیره کنید

7. در Cloudflare:
   - بعد از فعال شدن (چند دقیقه تا چند ساعت)
   - به بخش "DNS" بروید
   - رکوردهای زیر را اضافه کنید:
     - Type: A, Name: @, Content: 195.248.240.108, Proxy: Off (نارنجی نباشد)
     - Type: A, Name: www, Content: 195.248.240.108, Proxy: Off

---

### مرحله 4: بررسی DNS

بعد از تنظیم DNS، چند دقیقه تا چند ساعت طول می‌کشد تا DNS propagate شود.

**بررسی از کامپیوتر خود:**

```bash
# Windows (PowerShell یا CMD)
nslookup cutup.shop

# Linux/Mac
dig cutup.shop
# یا
nslookup cutup.shop
```

**باید IP سرور (195.248.240.108) را نشان دهد.**

---

## 🔍 عکس‌های راهنما (توضیحی)

### در پنل پارس پک:

1. **بخش مدیریت دامنه:**
   ```
   دامنه‌های من → cutup.shop → مدیریت DNS
   ```

2. **فرم اضافه کردن رکورد:**
   ```
   Type: [Dropdown] → A را انتخاب کنید
   Name: @ (یا خالی)
   Value: 195.248.240.108
   TTL: 3600
   ```

3. **بعد از اضافه کردن:**
   - رکورد @ (یا cutup.shop)
   - رکورد www
   - هر دو باید IP یکسان داشته باشند: 195.248.240.108

---

## ⚠️ نکات مهم

1. **TTL:** معمولاً 3600 (1 ساعت) کافی است
2. **Propagation:** ممکن است 5 دقیقه تا 24 ساعت طول بکشد
3. **بررسی:** می‌توانید از [whatsmydns.net](https://www.whatsmydns.net/#A/cutup.shop) استفاده کنید
4. **Cache:** اگر DNS تغییر نکرد، cache مرورگر را پاک کنید

---

## 🆘 اگر مشکل داشتید

### مشکل: DNS تغییر نمی‌کند
- چند ساعت صبر کنید
- Cache DNS را پاک کنید: `ipconfig /flushdns` (Windows)
- از DNS عمومی استفاده کنید: 8.8.8.8 (Google)

### مشکل: نمی‌توانم DNS را ویرایش کنم
- با پشتیبانی پارس پک تماس بگیرید
- یا از Cloudflare استفاده کنید (رایگان و بهتر)

### مشکل: Nameserver را نمی‌دانم چطور تغییر دهم
- در پنل پارس پک، به بخش "تنظیمات دامنه" بروید
- "Nameservers" یا "تنظیمات Nameserver" را پیدا کنید
- Nameserver های Cloudflare را وارد کنید

---

## ✅ بعد از تنظیم DNS

بعد از اینکه DNS propagate شد (nslookup IP درست را نشان داد):

1. **در سرور، SSL Certificate را دریافت کنید:**
   ```bash
   certbot --nginx -d cutup.shop -d www.cutup.shop
   ```

2. **فایل‌های website را در سرور قرار دهید:**
   ```bash
   cd /var/www/cutup
   # فایل‌های website را در پوشه website قرار دهید
   ```

3. **CORS را در backend تنظیم کنید**

---

## 📞 پشتیبانی

اگر مشکلی داشتید:
- پشتیبانی پارس پک: از طریق تیکت در پنل
- یا از Cloudflare استفاده کنید (رایگان و ساده‌تر)

