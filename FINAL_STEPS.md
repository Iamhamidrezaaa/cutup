# مراحل نهایی - رفع خطای 401

## ⚠️ خطای "No such renderer"

این خطا یک warning است و معمولاً مشکلی ایجاد نمی‌کند. می‌توانید آن را نادیده بگیرید.

## 🔍 بررسی مشکل اصلی (401)

خطای 401 یعنی API Key در runtime در دسترس نیست. برای رفع:

### 1. بررسی Environment Variables

در Vercel Dashboard:

1. به **Settings** → **Environment Variables** بروید
2. بررسی کنید که `OPENAI_API_KEY` وجود دارد
3. مطمئن شوید که برای **Production** تنظیم شده

**باید ببینید:**
- Key: `OPENAI_API_KEY`
- Environments: باید شامل `Production` باشد

### 2. اگر وجود ندارد یا فقط Development است:

1. روی **"Add New"** کلیک کنید
2. Key: `OPENAI_API_KEY`
3. Value: `YOUR_OPENAI_API_KEY`
4. Environments: **Production** را انتخاب کنید
5. روی **"Save"** کلیک کنید

### 3. Redeploy (مهم!)

بعد از تنظیم Environment Variable:

1. به **Deployments** بروید
2. روی deployment **Current** (`HEfp1Br3o`) کلیک کنید
3. روی **"Redeploy"** کلیک کنید
4. منتظر بمانید تا deployment کامل شود

**⚠️ مهم**: Environment Variables فقط در deployment بعدی اعمال می‌شوند.

### 4. بررسی لاگ‌ها

بعد از redeploy:

1. به deployment جدید بروید
2. به تب **"Logs"** بروید
3. یک درخواست تست بفرستید (از افزونه)
4. لاگ‌ها را بررسی کنید

**باید ببینید:**
```
HAS_KEY true
KEY_PREFIX sk-proj
TRANSCRIBE: Environment check: {
  apiKeyPresent: true,
  apiKeyPrefix: "sk-proj-..."
}
```

اگر `HAS_KEY false` باشد، یعنی Environment Variable درست تنظیم نشده.

## 🧪 تست

بعد از redeploy:

1. افزونه را reload کنید
2. یک فایل صوتی انتخاب کنید
3. روی "خلاصه‌سازی" کلیک کنید
4. Console را باز کنید (F12)

**اگر موفق شد:**
- باید خطای 401 برطرف شود
- Transcription انجام می‌شود
- سپس Summarization انجام می‌شود

**اگر هنوز 401 می‌دهد:**
- Environment Variables را دوباره بررسی کنید
- مطمئن شوید که برای Production تنظیم شده
- دوباره redeploy کنید

## 📋 چک‌لیست نهایی

- [ ] Settings → Environment Variables بررسی شده
- [ ] `OPENAI_API_KEY` برای Production وجود دارد
- [ ] Redeploy انجام شده
- [ ] لاگ‌ها بررسی شده (`HAS_KEY true`)
- [ ] افزونه reload شده
- [ ] تست انجام شده

## 🎯 بعد از رفع مشکل

وقتی همه چیز کار کرد:
1. یک فایل صوتی واقعی تست کنید
2. منتظر نتیجه بمانید (30-60 ثانیه)
3. اگر موفق بود، آماده است! 🎉

