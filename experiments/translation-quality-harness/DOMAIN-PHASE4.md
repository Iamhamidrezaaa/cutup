# Phase 4 — Domain Intelligence (reference)

Domain detection runs before translation in `translate-srt.js`. See `api/domain-detection.js` and `api/domain-translation-hints.js`.

Harness report includes `domainAccuracy` and `byDomain` when running `run-harness.mjs`.
