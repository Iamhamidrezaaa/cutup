# رفع خطای 413 - Payload Too Large

## 🔍 مشکل

خطای 413 یعنی فایل صوتی خیلی بزرگ است. این می‌تواند به دلایل زیر باشد:

1. **فایل واقعاً بزرگ است** (بیشتر از 4.5MB)
2. **Data URL خیلی بزرگ می‌شود** (base64 encoding فایل را 33% بزرگتر می‌کند)
3. **Vercel Function limit** (حداکثر 4.5MB برای request body)

## ✅ راه‌حل‌ها

### راه‌حل 1: محدود کردن اندازه فایل (ساده‌ترین)

در `popup.js`، قبل از آپلود فایل، اندازه آن را بررسی کنید:

```javascript
// در handleFileSelect یا handleSummarize
if (file.size > 4 * 1024 * 1024) { // 4MB
  alert('فایل خیلی بزرگ است. لطفاً فایلی کمتر از 4MB انتخاب کنید.');
  return;
}
```

### راه‌حل 2: استفاده از endpoint upload (بهتر)

به جای ارسال مستقیم data URL، فایل را به endpoint `/api/upload` بفرستید و سپس URL را استفاده کنید.

### راه‌حل 3: فشرده‌سازی فایل (پیشرفته)

فایل را قبل از ارسال فشرده کنید (نیاز به library اضافی).

## 🔧 پیاده‌سازی راه‌حل 1 (ساده)

اضافه کردن بررسی اندازه فایل در `popup.js`.

## 📋 چک‌لیست

- [ ] بررسی اندازه فایل اضافه شده
- [ ] پیام خطای مناسب نمایش داده می‌شود
- [ ] فایل‌های بزرگ رد می‌شوند

## 🎯 بعد از رفع مشکل

وقتی اندازه فایل را محدود کردید:
1. فایل‌های کوچک باید کار کنند
2. فایل‌های بزرگ باید رد شوند با پیام مناسب

