# رفع مشکل همگام‌سازی Session

## 🔍 مشکل

وقتی کاربر در Dashboard لاگین می‌کند، session در localStorage ذخیره می‌شود اما افزونه آن را نمی‌بیند.

## ✅ راه‌حل

استفاده از **Content Script** که می‌تواند به localStorage سایت دسترسی داشته باشد و session را به extension بفرستد.

### فایل‌های جدید:
- `content-script.js` - Content script برای sync کردن session
- `background.js` - Background service worker (به‌روزرسانی شده)

### فایل‌های ویرایش شده:
- `manifest.json` - اضافه شدن content_scripts
- `popup.js` - بهبود listener برای session updates
- `website/dashboard.js` - ساده‌سازی (content script خودش sync می‌کند)

---

## 📝 نحوه کار

1. **Content Script** روی صفحات `cutup.shop` اجرا می‌شود
2. وقتی session در localStorage تغییر می‌کند، content script آن را به background script می‌فرستد
3. **Background Script** session را در `chrome.storage.local` ذخیره می‌کند
4. **Popup** با `chrome.storage.onChanged` تغییرات را تشخیص می‌دهد
5. Popup session را verify می‌کند و پروفایل کاربر را نمایش می‌دهد

---

## 🔧 بعد از آپلود

1. **افزونه را reload کنید:**
   - به `chrome://extensions/` بروید
   - روی "Reload" کلیک کنید

2. **صفحه Dashboard را refresh کنید:**
   - `Ctrl+Shift+R` (یا `Cmd+Shift+R`)

3. **تست کنید:**
   - در Dashboard لاگین کنید
   - افزونه را باز کنید
   - باید لاگین شده باشد و پیام خوش‌آمدگویی نمایش داده شود

---

## ⚠️ نکات

- Content script فقط روی صفحات `cutup.shop` اجرا می‌شود
- اگر افزونه را reload نکنید، content script لود نمی‌شود
- اگر مشکل داشتید، console مرورگر را چک کنید (F12)

