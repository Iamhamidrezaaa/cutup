# راهنمای Deploy آپدیت صفحه اصلی

## 📋 فایل‌های تغییر یافته

- `website/index.html` - آپدیت عنوان و محتوا
- `website/style.css` - استایل برای اسکرین‌شات
- `website/extension-screenshot.png` - اسکرین‌شات افزونه (باید اضافه شود)

---

## 🚀 مراحل Deploy

### مرحله 1: آماده‌سازی اسکرین‌شات

1. افزونه را باز کنید
2. یک اسکرین‌شات از رابط کاربری بگیرید (مثلاً در حال خلاصه‌سازی)
3. فایل را با نام `extension-screenshot.png` ذخیره کنید
4. فایل را در پوشه `website/` قرار دهید

**نکته:** اگر اسکرین‌شات ندارید، می‌توانید بعداً اضافه کنید. صفحه یک placeholder نمایش می‌دهد.

---

### مرحله 2: Commit و Push به Git

```bash
# رفتن به دایرکتوری پروژه
cd "E:\Machine Learning\cutup"

# اضافه کردن فایل‌های تغییر یافته
git add website/index.html website/style.css

# اگر اسکرین‌شات را اضافه کردید:
git add website/extension-screenshot.png

# Commit
git commit -m "Update homepage with download features and extension screenshot

- Change title to 'دانلود و خلاصه‌سازی هوشمند ویدئو و موزیک'
- Add download video and music features to homepage
- Update features section with new capabilities
- Add extension screenshot in hero section
- Update how-it-works section"

# Push به GitHub
git push origin main
```

---

### مرحله 3: آپلود فایل‌ها به سرور

**روش 1: استفاده از WinSCP (پیشنهادی)**

1. WinSCP را باز کنید
2. اتصال به سرور:
   - Host: `195.248.240.108`
   - Username: `root`
   - Password: (رمز سرور)
   - Protocol: `SFTP`

3. فایل‌های زیر را به `/var/www/cutup/website/` آپلود کنید:
   - `website/index.html`
   - `website/style.css`
   - `website/extension-screenshot.png` (اگر دارید)

**روش 2: استفاده از SCP (از Git Bash)**

```bash
# از Git Bash
scp website/index.html root@195.248.240.108:/var/www/cutup/website/
scp website/style.css root@195.248.240.108:/var/www/cutup/website/
scp website/extension-screenshot.png root@195.248.240.108:/var/www/cutup/website/
```

---

### مرحله 4: بررسی در سرور (اختیاری)

```bash
# اتصال به سرور
ssh root@195.248.240.108

# رفتن به دایرکتوری
cd /var/www/cutup/website

# بررسی فایل‌ها
ls -la index.html style.css extension-screenshot.png

# بررسی محتوای فایل (اختیاری)
cat index.html | head -20
```

---

### مرحله 5: Reload Nginx

```bash
# اتصال به سرور
ssh root@195.248.240.108

# Reload Nginx (برای پاک کردن cache)
systemctl reload nginx

# یا اگر reload کار نکرد:
systemctl restart nginx

# بررسی وضعیت
systemctl status nginx
```

**نکته مهم:** 
- ❌ **PM2 لازم نیست** - PM2 فقط برای backend API (`server.js`) است
- ✅ **فقط Nginx reload کافی است** - فایل‌های `website/` توسط Nginx serve می‌شوند
- اگر از Nginx استفاده نمی‌کنید، فایل‌ها مستقیماً خوانده می‌شوند و نیازی به restart نیست

---

## ✅ چک‌لیست

- [ ] اسکرین‌شات افزونه آماده شده (`extension-screenshot.png`)
- [ ] فایل‌ها commit شده‌اند
- [ ] فایل‌ها push شده‌اند
- [ ] فایل‌ها به سرور آپلود شده‌اند
- [ ] Nginx reload شده است (برای پاک کردن cache)
- [ ] صفحه اصلی در مرورگر تست شده است

---

## 🧪 تست

بعد از آپلود، صفحه اصلی را در مرورگر باز کنید:

- اگر از دامنه استفاده می‌کنید: `https://cutup.shop`
- اگر از IP استفاده می‌کنید: `http://195.248.240.108` (اگر Nginx تنظیم شده)

**چیزهایی که باید بررسی کنید:**
- ✅ عنوان جدید نمایش داده می‌شود
- ✅ بخش ویژگی‌ها شامل دانلود ویدئو و موزیک است
- ✅ اسکرین‌شات افزونه نمایش داده می‌شود (یا placeholder)
- ✅ استایل‌ها درست اعمال شده‌اند

---

## 🆘 عیب‌یابی

### اگر اسکرین‌شات نمایش داده نمی‌شود:
- بررسی کنید فایل `extension-screenshot.png` در پوشه `website/` سرور وجود دارد
- بررسی کنید نام فایل دقیقاً `extension-screenshot.png` است
- بررسی کنید دسترسی فایل درست است: `chmod 644 website/extension-screenshot.png`

### اگر تغییرات نمایش داده نمی‌شوند:
- Cache مرورگر را پاک کنید (Ctrl+Shift+R یا Ctrl+F5)
- بررسی کنید فایل‌ها درست آپلود شده‌اند
- بررسی کنید Nginx reload شده است
- بررسی کنید Nginx cache را پاک کرده است: `systemctl reload nginx`

---

## 📝 نکات مهم

1. **اسکرین‌شات:** اگر الان اسکرین‌شات ندارید، می‌توانید بعداً اضافه کنید. صفحه یک placeholder نمایش می‌دهد.

2. **Cache:** بعد از آپلود، حتماً cache مرورگر را پاک کنید تا تغییرات را ببینید.

3. **PM2 vs Nginx:**
   - ❌ **PM2 لازم نیست** - PM2 فقط برای backend API (`server.js`) است که روی پورت 3001 اجرا می‌شود
   - ✅ **فقط Nginx reload کافی است** - فایل‌های `website/` مستقیماً توسط Nginx از `/var/www/cutup/website` خوانده می‌شوند
   - اگر فایل‌های `api/` یا `server.js` را تغییر دهید، آن وقت باید PM2 را restart کنید

4. **Backup:** قبل از آپلود، می‌توانید از فایل‌های قبلی backup بگیرید:
   ```bash
   ssh root@195.248.240.108
   cd /var/www/cutup/website
   cp index.html index.html.backup
   cp style.css style.css.backup
   ```

