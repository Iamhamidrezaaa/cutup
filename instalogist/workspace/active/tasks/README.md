# Active tasks

Place open task files here. One file per task; include Instalogist Task v1 YAML frontmatter (`task_id`, `owner_agent`, `status`, `priority`, `risk_class`, `updated_at`).

When `status` is `done` or `cancelled`, move the file to `archive/tasks/` (optionally under `archive/tasks/YYYY/`).
