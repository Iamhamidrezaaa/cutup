# Active incidents

One markdown file per incident. Track `severity`, `owner_agent`, and a `timeline` section. Close with `resolution_summary` or `postmortem`, then move to `archive/incidents/` (e.g. `archive/incidents/YYYY/`).
