{
  "name": "@instalogist/orchestrator",
  "version": "0.1.0",
  "private": true,
  "description": "Instalogist operational orchestration (task queue, agents, lifecycle, approvals) — no autonomous deploy",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "test": "node --test dist/**/*.test.js 2>nul || node --import tsx --test src/**/*.test.ts"
  },
  "devDependencies": {
    "typescript": "~5.6.3"
  }
}
