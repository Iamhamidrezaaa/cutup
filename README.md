# Cutup - خلاصه‌ساز هوشمند

افزونه کروم برای تبدیل ویدئو و صوت به متن و خلاصه‌سازی هوشمند با AI

## ویژگی‌ها

- 🎥 پشتیبانی از لینک‌های یوتیوب
- 🎵 پشتیبانی از فایل‌های صوتی
- 🧠 تبدیل صوت به متن با Whisper AI
- 📝 خلاصه‌سازی هوشمند با GPT
- 🌓 حالت Dark/Light
- 💾 ذخیره تاریخچه در Local Storage

## نصب (نسخه Unpacked)

1. این مخزن را کلون کنید:
```bash
git clone <repository-url>
cd cutup
```

2. مرورگر Chrome را باز کنید و به `chrome://extensions/` بروید

3. "Developer mode" را فعال کنید

4. روی "Load unpacked" کلیک کنید و پوشه `cutup` را انتخاب کنید

## ساختار پروژه

```
cutup/
├── manifest.json          # تنظیمات افزونه
├── popup.html            # رابط کاربری
├── popup.css             # استایل‌ها
├── popup.js              # منطق اصلی
├── background.js         # Service Worker
├── icons/                # آیکون‌های افزونه
│   ├── icon16.png
│   ├── icon48.png
│   └── icon128.png
└── api/                  # Backend API (Vercel)
    ├── transcribe.js
    └── summarize.js
```

## Backend Setup

برای استفاده از افزونه، باید API را روی Vercel یا Render راه‌اندازی کنید.

### متغیرهای محیطی مورد نیاز:

- `OPENAI_API_KEY` - برای خلاصه‌سازی با GPT
- `WHISPER_API_KEY` - برای تبدیل صوت به متن (یا استفاده از OpenAI Whisper)

### تنظیم API URL

API URL در فایل `popup.js` تنظیم شده است:

```javascript
const API_BASE_URL = 'https://cutup-a0p9oqk9z-hamidreza-askarizadehs-projects.vercel.app';
```

## توسعه

### نیازمندی‌ها

- Chrome Browser (نسخه 88+)
- Node.js (برای Backend)

### تست

1. افزونه را در حالت Developer mode نصب کنید
2. روی آیکون افزونه کلیک کنید
3. لینک یوتیوب یا فایل صوتی را وارد کنید
4. روی "خلاصه‌سازی" کلیک کنید

## مجوزها

- `storage`: برای ذخیره تاریخچه و تنظیمات
- `activeTab`: برای دسترسی به تب فعال
- `host_permissions`: برای دسترسی به لینک‌های یوتیوب

## نسخه

1.0.0 - نسخه اولیه MVP

## لایسنس

MIT

