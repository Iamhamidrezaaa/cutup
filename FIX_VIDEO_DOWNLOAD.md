# رفع مشکل دانلود ویدئو

## 🔍 مشکلات

1. **ویدئو دانلود می‌شود اما قابل پخش نیست** - خطای "This item was encoded in a format that's not supported"
2. **بعد از logout، login prompt نمایش داده نمی‌شود**

## ✅ راه‌حل‌ها

### 1. رفع مشکل ویدئو

برای اینکه ویدئو با Windows Media Player سازگار باشد، باید از `--recode-video mp4` استفاده کنیم. این flag نیاز به **ffmpeg** دارد.

**مهم:** باید ffmpeg روی سرور نصب باشد!

### 2. رفع مشکل logout

- اضافه شدن listener برای حذف session در `chrome.storage.onChanged`
- فراخوانی `showLoginPrompt()` بعد از logout

---

## 🔧 نصب ffmpeg در سرور

```bash
# Ubuntu/Debian
sudo apt-get update
sudo apt-get install -y ffmpeg

# بررسی نصب
ffmpeg -version
```

---

## 📁 فایل‌های تغییر یافته

- `api/youtube-download.js` - اضافه شدن `--recode-video mp4` برای video
- `popup.js` - بهبود logout handler و storage listener

---

## 🚀 بعد از آپلود

### 1. نصب ffmpeg (اگر نصب نیست)

```bash
sudo apt-get update
sudo apt-get install -y ffmpeg
```

### 2. آپلود فایل‌ها

- `api/youtube-download.js`
- `popup.js`

### 3. Restart سرور

```bash
pm2 restart cutup-api
```

### 4. Reload افزونه

- به `chrome://extensions/` بروید
- روی "Reload" کلیک کنید

### 5. تست

1. **تست ویدئو:**
   - یک لینک یوتیوب وارد کنید
   - روی "دانلود ویدئو" کلیک کنید
   - کیفیت را انتخاب کنید
   - فایل باید دانلود شود و با Windows Media Player قابل پخش باشد

2. **تست logout:**
   - از dashboard خارج شوید
   - افزونه را باز کنید
   - باید login prompt نمایش داده شود

---

## ⚠️ نکات

- اگر ffmpeg نصب نباشد، ویدئو دانلود نمی‌شود و خطا می‌دهد
- `--recode-video mp4` ویدئو را به فرمت mp4 استاندارد تبدیل می‌کند که با همه media player ها سازگار است
- این فرآیند ممکن است کمی بیشتر طول بکشد (چون باید encode شود)

