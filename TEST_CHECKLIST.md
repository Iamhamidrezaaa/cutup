# چک‌لیست تست افزونه Cutup

## ✅ تنظیمات انجام شده

- [x] API_BASE_URL در `popup.js` تنظیم شده
- [x] API Key در `vercel.json` تنظیم شده
- [x] Backend در Vercel deploy شده

## 🧪 مراحل تست

### 1. نصب افزونه در Chrome

1. Chrome را باز کنید
2. به `chrome://extensions/` بروید
3. "Developer mode" را فعال کنید (گوشه بالا-راست)
4. روی "Load unpacked" کلیک کنید
5. پوشه `cutup` را انتخاب کنید

**بررسی**: افزونه باید در لیست نمایش داده شود و آیکون آن در نوار ابزار ظاهر شود.

### 2. تست UI اولیه

1. روی آیکون افزونه کلیک کنید
2. پنل popup باید باز شود

**بررسی**:
- [ ] رابط کاربری به درستی نمایش داده می‌شود
- [ ] فونت‌ها و استایل‌ها درست هستند
- [ ] دکمه "خلاصه‌سازی" غیرفعال است (تا زمانی که فایلی انتخاب نشده)

### 3. تست Dark/Light Mode

1. روی دکمه تغییر تم (🌙/☀️) کلیک کنید

**بررسی**:
- [ ] تم تغییر می‌کند
- [ ] رنگ‌ها درست نمایش داده می‌شوند
- [ ] تنظیمات ذخیره می‌شود (بعد از بستن و باز کردن دوباره)

### 4. تست آپلود فایل صوتی

1. روی "فایل صوتی را انتخاب کنید" کلیک کنید
2. یک فایل صوتی (MP3, WAV, M4A) انتخاب کنید

**بررسی**:
- [ ] نام فایل در input نمایش داده می‌شود
- [ ] دکمه "خلاصه‌سازی" فعال می‌شود

### 5. تست Paste از Clipboard

1. یک لینک یوتیوب را کپی کنید
2. روی دکمه 📋 کلیک کنید

**بررسی**:
- [ ] لینک در input paste می‌شود
- [ ] دکمه "خلاصه‌سازی" فعال می‌شود

### 6. تست خلاصه‌سازی (فایل صوتی)

1. یک فایل صوتی کوچک (کمتر از 5MB) انتخاب کنید
2. روی "خلاصه‌سازی" کلیک کنید

**بررسی**:
- [ ] دکمه به حالت loading می‌رود
- [ ] متن "در حال پردازش..." نمایش داده می‌شود
- [ ] بعد از 30-60 ثانیه، نتایج نمایش داده می‌شود

**نتایج باید شامل**:
- [ ] تب "نکات کلیدی" با 5-7 نکته
- [ ] تب "خلاصه" با یک پاراگراف خلاصه
- [ ] تب "متن کامل" با متن کامل transcription

### 7. تست Tab Switching

1. بعد از دریافت نتایج، روی تب‌های مختلف کلیک کنید

**بررسی**:
- [ ] هر تب محتوای مربوط به خود را نمایش می‌دهد
- [ ] تب فعال به درستی highlight می‌شود

### 8. تست Copy to Clipboard

1. روی یکی از تب‌ها بروید
2. روی دکمه 📋 (کپی) کلیک کنید

**بررسی**:
- [ ] دکمه به ✓ تغییر می‌کند
- [ ] محتوا در clipboard کپی می‌شود
- [ ] می‌توانید در جای دیگری paste کنید

### 9. تست تاریخچه

1. یک خلاصه دیگر بسازید
2. به بخش "تاریخچه" بروید

**بررسی**:
- [ ] خلاصه‌های قبلی در تاریخچه نمایش داده می‌شوند
- [ ] با کلیک روی یک آیتم، نتایج آن نمایش داده می‌شود

### 10. تست خطاها

#### تست با فایل خالی
1. بدون انتخاب فایل، روی "خلاصه‌سازی" کلیک کنید

**بررسی**: باید خطا نمایش داده شود

#### تست با لینک یوتیوب
1. یک لینک یوتیوب وارد کنید
2. روی "خلاصه‌سازی" کلیک کنید

**بررسی**: باید پیام خطا نمایش داده شود (چون استخراج یوتیوب پیاده‌سازی نشده)

## 🐛 مشکلات احتمالی

### خطای CORS
- **علت**: API در Vercel deploy نشده یا URL اشتباه است
- **راه حل**: Console را باز کنید (F12) و خطاها را بررسی کنید

### خطای "API Key is not set"
- **علت**: API Key در Vercel تنظیم نشده
- **راه حل**: `vercel env add OPENAI_API_KEY production` را اجرا کنید

### خطای "Failed to fetch"
- **علت**: URL API اشتباه است یا API در دسترس نیست
- **راه حل**: URL را در `popup.js` بررسی کنید

### فایل آپلود نمی‌شود
- **علت**: فایل خیلی بزرگ است یا فرمت پشتیبانی نمی‌شود
- **راه حل**: فایل کوچکتر یا فرمت دیگری امتحان کنید

## 📊 تست‌های پیشنهادی

### فایل‌های تست
- [ ] فایل صوتی کوتاه (30 ثانیه)
- [ ] فایل صوتی متوسط (2-3 دقیقه)
- [ ] فایل صوتی فارسی
- [ ] فایل صوتی انگلیسی
- [ ] فایل با کیفیت پایین
- [ ] فایل با کیفیت بالا

### سناریوهای تست
- [ ] تست با چند فایل پشت سر هم
- [ ] تست با بستن و باز کردن افزونه
- [ ] تست با تغییر تم در حین پردازش
- [ ] تست با فایل‌های بزرگ (بیش از 10MB)

## ✅ بعد از تست موفق

اگر همه تست‌ها موفق بودند:
1. [ ] آیکون‌های واقعی را بسازید
2. [ ] README را به‌روز کنید
3. [ ] برای انتشار در Chrome Web Store آماده شوید

