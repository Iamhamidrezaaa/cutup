# Cloudflare چیست و چه کاری می‌کند؟

## 🌐 Cloudflare چیست؟

Cloudflare یک سرویس **CDN (Content Delivery Network)** و **DNS Management** است که بین کاربران و سرور شما قرار می‌گیرد.

---

## 🔄 Cloudflare چه کاری می‌کند؟

### 1. **DNS Management (مدیریت DNS)**
- وقتی کاربر `cutup.shop` را در مرورگر تایپ می‌کند، Cloudflare به او می‌گوید که IP سرور شما `195.248.240.108` است
- این کار را خیلی سریع انجام می‌دهد (چون سرورهای Cloudflare در سراسر جهان هستند)

### 2. **CDN (Content Delivery Network)**
- اگر Proxy روشن باشد (نارنجی)، Cloudflare محتوای استاتیک (عکس، CSS، JS) را cache می‌کند
- این باعث می‌شود سایت شما سریع‌تر لود شود
- **اما برای API ما، Proxy باید Off باشد** چون می‌خواهیم مستقیماً به سرور وصل شویم

### 3. **DDoS Protection (محافظت از حملات)**
- Cloudflare از سایت شما در برابر حملات DDoS محافظت می‌کند
- ترافیک مشکوک را فیلتر می‌کند

### 4. **SSL/TLS Certificate (گواهینامه SSL)**
- Cloudflare می‌تواند SSL Certificate رایگان ارائه دهد
- اما ما از Let's Encrypt روی سرور خود استفاده می‌کنیم

---

## ⚙️ تنظیمات مهم در Cloudflare

### Proxy: On vs Off

**Proxy: On (نارنجی) - برای Frontend:**
- ✅ خوب برای: عکس، CSS، JS (فایل‌های استاتیک)
- ✅ Cache می‌کند → سریع‌تر
- ❌ بد برای: API (چون باید مستقیماً به سرور وصل شود)

**Proxy: Off (خاکستری) - برای API:**
- ✅ خوب برای: API endpoints (`/api/*`)
- ✅ مستقیماً به سرور شما وصل می‌شود
- ✅ برای SSL Certificate روی سرور لازم است

---

## 📊 جریان ترافیک

### بدون Cloudflare:
```
کاربر → cutup.shop → 195.248.240.108 (سرور شما)
```

### با Cloudflare (Proxy Off):
```
کاربر → cutup.shop → Cloudflare DNS → 195.248.240.108 (سرور شما)
```

### با Cloudflare (Proxy On):
```
کاربر → cutup.shop → Cloudflare CDN → (اگر cache شده) → Cloudflare
                                              ↓
                                    (اگر cache نشده) → 195.248.240.108
```

---

## 🎯 برای پروژه Cutup

### تنظیمات ما:
1. **Frontend (cutup.shop):** Proxy می‌تواند On باشد (اما فعلاً Off می‌گذاریم)
2. **API (/api/*):** Proxy باید **حتماً Off** باشد
3. **SSL:** از Let's Encrypt روی سرور استفاده می‌کنیم

### چرا Proxy برای API باید Off باشد؟
- API ما باید مستقیماً به سرور وصل شود
- برای SSL Certificate روی سرور (Let's Encrypt) لازم است
- برای دریافت فایل‌های بزرگ (ویدئو/صوت) بهتر است

---

## ✅ خلاصه

**Cloudflare برای ما:**
- ✅ مدیریت DNS (سریع و قابل اعتماد)
- ✅ محافظت از DDoS
- ❌ CDN/Cache (برای API استفاده نمی‌کنیم)
- ❌ SSL از Cloudflare (از Let's Encrypt استفاده می‌کنیم)

**نتیجه:** Cloudflare فقط DNS را مدیریت می‌کند و ترافیک را مستقیماً به سرور شما هدایت می‌کند.

