# آپلود فایل‌های همگام‌سازی Session

## 📁 فایل‌های جدید که باید آپلود شوند

### در افزونه:
- `content-script.js` ⭐ **مهم**
- `background.js` (به‌روزرسانی شده)

### در سایت:
- `website/dashboard.js` (به‌روزرسانی شده)

### فایل‌های ویرایش شده:
- `manifest.json` ⭐ **مهم** (اضافه شدن content_scripts)
- `popup.js` (بهبود listener)

---

## 🔧 مراحل

### 1. آپلود فایل‌ها

از WinSCP فایل‌های بالا را آپلود کنید.

**مهم:** حتماً `manifest.json` و `content-script.js` را آپلود کنید!

### 2. Reload افزونه

**خیلی مهم:** بعد از آپلود `manifest.json` و `content-script.js`:

1. به `chrome://extensions/` بروید
2. روی دکمه **"Reload"** افزونه Cutup کلیک کنید
3. یا افزونه را disable و دوباره enable کنید

**بدون reload، content script لود نمی‌شود!**

### 3. Refresh صفحه Dashboard

1. به `https://cutup.shop/dashboard.html` بروید
2. `Ctrl+Shift+R` بزنید (hard refresh)

### 4. تست

1. در Dashboard لاگین کنید (اگر لاگین نیستید)
2. افزونه را باز کنید
3. باید:
   - لاگین شده باشد
   - پروفایل کاربر نمایش داده شود
   - پیام "خوش آمدید [نام کاربر]!" نمایش داده شود
   - Login prompt نمایش داده نشود

---

## 🔍 عیب‌یابی

### اگر هنوز کار نمی‌کند:

1. **Console مرورگر را چک کنید (F12):**
   - باید پیام‌های `Background: Session synced...` را ببینید
   - باید پیام‌های `Popup: Session changed...` را ببینید

2. **بررسی کنید که content script لود شده:**
   - در Console مرورگر (در صفحه Dashboard):
   - باید پیام‌های مربوط به content script را ببینید

3. **بررسی manifest.json:**
   - مطمئن شوید که `content_scripts` اضافه شده است

4. **افزونه را کامل reload کنید:**
   - Disable کنید
   - Enable کنید
   - یا از Developer mode استفاده کنید

---

## ✅ چک‌لیست

- [ ] `content-script.js` آپلود شده
- [ ] `background.js` آپلود شده
- [ ] `manifest.json` آپلود شده (با content_scripts)
- [ ] `popup.js` آپلود شده
- [ ] `website/dashboard.js` آپلود شده
- [ ] افزونه reload شده
- [ ] صفحه Dashboard refresh شده
- [ ] تست انجام شده

